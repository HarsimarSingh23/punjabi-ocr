# Backend origin the frontend should call.
# Leave empty for local dev (Vite proxy) or when FastAPI serves the build.
# Set to your deployed backend URL on Cloudflare Pages (build-time variable).
VITE_API_BASE=
